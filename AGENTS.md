# AGENTS.md

This file intentionally mirrors the project instructions in `AGENT`.

The current implementation target is Stage 1 only:

* Use fake object poses.
* Use fake BODex grasp targets.
* Use a simple mock planner.
* Use a simple mock executor.
* Save trajectory and evaluation results under `outputs/`.
* Do not install or integrate cuRobo, MuJoCo, YOLO, or BODex yet.

